namespace userconfig {
    export const ARCADE_SCREEN_WIDTH = 240
    export const ARCADE_SCREEN_HEIGHT = 200
}
tiles.setCurrentTilemap(tilemap`level8`)
scene.setBackgroundColor(9)
scene.centerCameraAt(18 * 18 / 2, 17 * 17 / 2)
